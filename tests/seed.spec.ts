import { test, expect } from '@playwright/test';

test.describe('Complete the product order process', () => {
  test('Complete the product order process', async ({ page }) => {
    // generate code here.
  });
});
